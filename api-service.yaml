---
apiVersion: v1
kind: Service
metadata:
  name: api-service
  labels:
    service: api
spec:
  selector:
    app: synergychat-api
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080

