---
apiVersion: v1
kind: Service
metadata:
  namespace: crawler
  name: crawler-service
  labels:
    service: api
spec:
  selector:
    app: synergychat-crawler
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080

